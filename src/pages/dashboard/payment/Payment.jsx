import SectionTitle from "../../../components/sectionTitle/SectionTitle";


const Payment = () => {

    return (
        <div>
            <SectionTitle heading="Payment" subHeading="Continue to payment" />
        </div>
    );
};

export default Payment;